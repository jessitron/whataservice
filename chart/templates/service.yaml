apiVersion: v1
kind: Service
metadata:
  name: {{ include "whataservice.fullname" . }}
  labels:
    {{- include "whataservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whataservice.selectorLabels" . | nindent 4 }}
